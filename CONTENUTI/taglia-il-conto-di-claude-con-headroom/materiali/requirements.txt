# Dipendenze del mini-progetto Headroom.
# Installa tutto con:  pip install -r requirements.txt

flask>=3.0
openai>=1.40
python-dotenv>=1.0

# Headroom. L'installazione base basta per la demo: la compressione dei DATI
# (JSON, log, telemetria) e' pura Python, non scarica nulla ed e' istantanea.
# E' da li' che arriva il taglio grosso (e senza perdere informazione).
headroom-ai>=0.27

# (Opzionale) Per provare a comprimere anche la PROSA con il modello ML di
# Headroom, installa l'extra: pip install "headroom-ai[ml]"  (scarica ~GB di
# dipendenze). Non serve per la demo: codice e testo restano comunque leggibili.
