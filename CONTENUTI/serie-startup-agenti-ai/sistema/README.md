# sistema/  (staging del prodotto Competitor Analyzer)

Bozze del prodotto reale che la serie racconta: landing page, schema Supabase, edge function di signup,
template `BRIEF.md`, pipeline agenti adattata dal sistema esistente.

**TEMPORANEO**: questa cartella è staging. Quando il prodotto prende forma si sposta in una **repo GitHub
dedicata**, separata da `yt-content-creation`.

Spec completa, architettura e ordine di costruzione: vedi `../CLAUDE.md` §2-§3.
Riusa il sistema in `../../claude-4-8-dynamic-workflows-10-agenti-paralleli/materiali/`.

Ancora da popolare. Primo pezzo da costruire: **landing page + form + signup Supabase**.
