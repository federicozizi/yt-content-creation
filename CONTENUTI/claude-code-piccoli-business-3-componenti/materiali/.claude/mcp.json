{
  "_comment": "Configurazione dei connettori MCP per questo progetto. Gemello inglese: CONTENUTI/claude-code-small-business-3-components/materiali/.claude/mcp.json",
  "_note": "Le credenziali NON vanno scritte qui. Vengono lette da .env tramite ${VAR}. Modifica solo se sai cosa stai facendo.",

  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server-supabase"],
      "env": {
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_SERVICE_ROLE_KEY": "${SUPABASE_SERVICE_ROLE_KEY}"
      }
    }
  }
}
